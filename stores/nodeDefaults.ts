import {
    defaultBlendNodeData
} from "./blendNode";

import {
    defaultColorMatrixNodeData
} from "./colorMatrixNode";

export const defaultNodeData = {
    blend: defaultBlendNodeData,
    colorMatrix: defaultColorMatrixNodeData,
};